---
title: "Hello, World — 部落格上線了"
date: "2026-06-22"
description: "用 Next.js 從零打造、之後要部署到 GCP Cloud Run 的個人部落格。"
tags: ["meta", "nextjs"]
---

歡迎來到我的部落格！這是第一篇文章 👋

這個站台用 **Next.js（App Router）** 打造，文章以 Markdown/MDX 存放在 repo 裡，未來會：

- 用 Docker 容器化
- 部署到 **GCP Cloud Run**
- 接上自訂 domain
- 用 GitHub Actions 做 CICD

## 程式碼也能正常顯示

```ts
function greet(name: string) {
  return `Hello, ${name}!`;
}
```

## 表格（GitHub-flavored Markdown）

| 階段 | 內容 |
| --- | --- |
| Phase 0 | 本機把 blog 跑起來 |
| Phase 2 | 部署到 Cloud Run |

敬請期待後續的雲端筆記。
